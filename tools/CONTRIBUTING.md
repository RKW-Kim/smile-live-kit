# Contributing

Operators never hand-edit files in core/. Change via brand packs, data layer, or core PRs.

- Brand changes: PR to `brands/<channel>/brand.json`
- Data: edit `core/bridge.js` providers
- Layouts: edit `brands/template/brand.json` -> layouts
- No images/videos in PRs — use CSS/SVG
